# תוכנית אבטחה — Mas4U (שלב ב')

מסמך זה מסביר איך נהפוך את האתר למאובטח לפני שמעלים נתוני לקוחות אמיתיים
(תעודות זהות, נתונים פיננסיים וכו').

## המצב היום (שלב א')

האתר עובד מול מסד נתונים משותף ב‑Supabase. כרגע ההתחברות היא "פשוטה"
(אימייל + קוד אישי שנשמר בתוך הנתונים), והגישה למסד הנתונים פתוחה למי שמחזיק
במפתח הציבורי (anon key) — כלומר, תיאורטית, כל מי שמגיע לכתובת האתר. זה בסדר
להרצה עם נתוני דמו, אבל **לא מספיק לנתוני לקוחות אמיתיים.**

## המטרה (שלב ב')

לדרוש **התחברות אמיתית עם סיסמה** (דרך מערכת האימות המובנית של Supabase, חינמית),
ולנעול את מסד הנתונים כך ש**רק משתמשים מחוברים** יכולים לקרוא או לכתוב מידע.
אחרי זה, המפתח הציבורי לבדו כבר לא נותן גישה לנתונים.

## למה לא הפעלתי את זה לבד בלילה

שינוי ההתחברות והנעילה הוא צעד "הכל או כלום": אם מפעילים אותו וטועים במשהו,
אפשר לנעול את כולם בחוץ — כולל אותך. בנוסף, מסביבת העבודה שלי אני חסום מלגשת
ל‑Supabase שלך, אז אני לא יכול לבדוק שהשינוי עובד לפני שהוא עולה. לכן בניתי את
כל התשתית והתוכנית, ואת ההפעלה בפועל נעשה יחד (5–10 דקות), עם בדיקה שכל אחד
מצליח להתחבר.

## מה כבר מוכן בקוד

- `src/lib/auth.ts` — פונקציות התחברות/התנתקות מול Supabase Auth (מוכנות, לא פעילות עדיין).
- `supabase_secure_rls.sql` — פקודות שנועלות את מסד הנתונים למשתמשים מחוברים בלבד.

## שלבי ההפעלה (נעשה יחד)

1. **הפעלת אימות ב‑Supabase**
   ב‑Supabase → Authentication → Providers → לוודא ש‑Email מופעל.
   אם לא רוצים תהליך אישור מייל בהתחלה: Authentication → Sign In / Providers →
   לכבות "Confirm email" (אפשר להחזיר בהמשך).

2. **יצירת חשבונות לצוות**
   ב‑Supabase → Authentication → Users → "Add user", לכל איש צוות: אימייל + סיסמה.
   (או שאבנה מסך ניהול קטן שיוצר אותם מתוך האתר.)

3. **חיבור מסך ההתחברות ל‑Auth**
   אחליף את מסך ההתחברות כך שישתמש ב‑`auth.ts` (אימייל + סיסמה) במקום קוד אישי.
   נעלה גרסה חדשה ונוודא שאתה מצליח להתחבר עם חשבון בדיקה.

4. **נעילת מסד הנתונים**
   רק אחרי ששלב 3 עובד — נריץ את `supabase_secure_rls.sql` ב‑SQL Editor.
   מרגע זה, גישה ללא התחברות נחסמת.

5. **בדיקה סופית**
   התחברות מכמה מכשירים, ודא שהסנכרון בזמן אמת עדיין עובד, וש"גלישה בסתר"
   ללא התחברות לא מציגה נתונים.

## החלטות שנצטרך לקבל

- **לקוחות**: האם גם ללקוחות תהיה התחברות עם סיסמה (מאובטח יותר), או שנתחיל
  בנעילת צד המשרד בלבד ונטפל בלקוחות בשלב נפרד? (מומלץ: קודם לאבטח את צד המשרד,
  כי שם יושבים הנתונים הרגישים.)
- **איפוס סיסמה**: להפעיל "שכחתי סיסמה" במייל (Supabase תומך מובנה).

## מה יש לגבות לפני ההפעלה

לפני נעילה — כדאי לייצא גיבוי של הנתונים (אבנה כפתור "ייצוא גיבוי" שמוריד את כל
מסד הנתונים לקובץ), כדי שיהיה עותק ליתר ביטחון.

## תוכנית חזרה לאחור (Rollback)

אם משהו משתבש בהפעלה — בתחתית `supabase_secure_rls.sql` יש פקודות שמחזירות את
המצב הקודם (גישה פתוחה), כך שהאתר חוזר לעבוד מיד בזמן שאנחנו מתקנים.
